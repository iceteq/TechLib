/** How many images peek on the wall card (2 enables the split collage). */
export const NOTE_PREVIEW_IMAGE_LIMIT = 2;

/**
 * Wall-card image transform width (px). Supabase Image Transformations
 * (Pro+) serve a smaller JPEG/WebP; Free plans fall back to the full URL.
 */
export const WALL_IMAGE_PREVIEW_WIDTH = 640;
/** Wall-card transform quality (20–100). */
export const WALL_IMAGE_PREVIEW_QUALITY = 65;

export const APP_NAME = 'TechLib';
